<div class="flex justify-center gap-2 mt-2">
  <% cache(types.sort.join("-")) do %>
    <% types.each do |type| %>
      <span class="text-xs rounded-full px-2 py-1 font-semibold <%= type_badge_class(type) %>">
        <%= type.capitalize %>
      </span>
    <% end %>
  <% end %>
</div>
